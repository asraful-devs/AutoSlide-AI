import { Outlet } from 'react-router-dom';

const Workspace = () => {
    return (
        <div>
            MenubarasMenubarPrimitive
            <Outlet />
        </div>
    );
};

export default Workspace;
